services:
  simple:
    build:
      context: .
    environment:
      NODE_ENV: production
    ports:
      - 8080:80
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/hello"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
